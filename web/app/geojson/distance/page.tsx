import { Metadata } from "next";

export const metadata: Metadata = {
  title: "GeoJson Distance Calculator",
  description: "Calculate the distance with GeoJson data",
};

export default function GeoJsonDistanceCalculatorPage() {
  return <main></main>;
}
